class Node {
  constructor(element) {
    this.element = element;
    this.next = null; // 마지막 요소의 next는 항상 null;
    this.prev = null; // new
  }

module.exports = class DoublyLinkedList {
  length = 0;
  head = null;
  tail = null; // new

}