import { ICoordinates, makeICoordinates, makeRandomICoordinates } from './model/coordinates';

const coordinates: ICoordinates = makeRandomICoordinates();
const coordinates1: ICoordinates = makeRandomICoordinates();

console.log(coordinates);
console.log(coordinates1);
