import { Reset } from 'styled-reset';
import React from 'react';
import TextEditor from 'components/TextEditor';

function App() {
  return (
    <React.Fragment>
      <Reset />
      <TextEditor />
    </React.Fragment>
  );
}

export default App;
