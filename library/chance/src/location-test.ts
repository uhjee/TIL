import { makeRandomILocation, ILocation } from './model/location';

const location1: ILocation = makeRandomILocation();
console.log(location1);

const location2: ILocation = makeRandomILocation();
console.log(location2);
